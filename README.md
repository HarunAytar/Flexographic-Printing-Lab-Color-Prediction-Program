# Flexographic-Printing-Lab-Color-Prediction-Program
An AI prediction program, trained with the XGboost model, that predicts the Lab color of the print output based on the parameters of each print job on flexographic printing machines.
